# 初始化与协作规则（/init）

本文件用于约束本目录下的协作与输出规范。

## 语言规则

- 与用户沟通时，默认使用中文。
- 编写文档、说明、提交备注时，默认使用中文。
- 代码注释默认使用中文，保证表达清晰、简洁、可维护。

## 例外规则

- 涉及第三方协议字段、库接口、标准术语时，可保留英文原文。
- 命令行参数、环境变量、配置键名、代码标识符保持原始语义，不做强制中文化。

## 执行原则

- 在不改变功能的前提下优先保证可读性。
- 改动应聚焦当前任务，避免无关重构。
